# 「이름을 부르다」 — 독립운동가 숏츠 채널 PRD

잡학쿠키와 **별개 채널**이다. 소재의 무게가 다르므로 톤·포맷·규칙을 공유하지 않는다.
(잡학쿠키 파이프라인은 `PRD.md` 참고)

## 1. 컨셉

독립운동가 한 명을 소개하되, **정체를 마지막까지 숨겼다가 이름을 공개**하는 구조.
채널명이 곧 포맷 설명이 된다 — 매 영상 마지막이 채널명 각인 순간이 된다.

## 2. 대본 구조

```
훅(이름 숨김) → 채널명 → 정황 축적 → 힌트 → [0.5초 정적] → 이름 공개 → 착지 → 여운 → 추모 마무리
```

- **어투: `~했다` 평서체 단문.** 잡학쿠키의 음슴체(~함/~했음)는 **쓰지 않는다.**
  순국·고문·처형을 다루는데 음슴체는 경박하게 읽히고 유족 반발 소지가 있다.
- 훅 앞에 채널명 인트로를 두지 않는다. 숏츠는 0~2초가 훅이어야 하며,
  "안녕하세요 OO입니다"로 시작하면 이탈률이 급등한다.
- **⚠️ 채널명은 나레이션으로 읽지 않는다 (2026-09-18 변경, 11화부터).**
  전에는 "이름을 부르다. 오늘 부를 이름."을 3초 지점에서 **낭독**했는데,
  유튜브 유지율에서 **7~8초에 30%가 이탈**했고 **10편 중 9편이 그 시점에 이 컷을
  재생 중**이었다. 훅은 7초까지 붙잡는 데 성공했는데, 훅이 끝나는 순간
  나레이션 2.5초 + 무음 1.4초 = **3.9초짜리 정보 공백**이 오는 구조였다.
  → **0.8초 사운드 로고(`scripts/make_logo.py`) + 화면 자막**으로 대체한다.
  브랜드 뒤 무음도 없앤다(반전 뒤 1.4초는 유지 — 거기까지 온 사람은 몰입 상태다).
  대본에서는 `"kind": "logo"`로 표시하고, 렌더러가 레벨(-30dB)·자막 처리를 달리한다.

### 훅은 통념 부정으로 연다 (2026-09-20 추가, 11화부터)

**`X는 Y가 아니다` → 본문이 답을 준다.** 사건을 그냥 서술하지 말고,
시청자가 당연하다고 여길 전제를 먼저 깬다.

| | 전 (사건 서술) | 후 (통념 부정) |
|---|---|---|
| 11화 | 1928년 5월, 대만 타이중 거리에서 일본 육군대장이 칼에 찔렸다 | **일왕의 장인을 찌른 사람은 군인도 독립군도 아니었다** |

⚠️ **훅을 바꾸면 반전(06번)도 따라 옮겨야 한다.** 훅에 정보를 올린 만큼
06번이 비므로, 훅의 부정문과 **호응하는 답**을 06번에 놓는다.
11화는 '대상(일왕의 장인)'이 훅으로 올라갔으므로 06번이 '직업·나이(가게 점원, 스물셋)'가 됐다.
훅의 "군인도 독립군도 아니었다"와 06번의 "가게 점원"이 짝을 이룬다.

⚠️ **소제목과 축이 겹치면 안 된다.** 11화 소제목이 '대만에서'(장소)이므로
훅의 부정은 장소가 아닌 다른 축(신분)을 쳐야 한다. 같은 축을 두 번 쓰면 김이 빠진다.

### ⚠️ 훅은 선언으로 연다 (2026-09-21 전면 개정, 12화부터)

**사용자 지시 — "반전 요소 같은 건 빼고 가장 흥미로운 걸 맨 앞에 빼자.
예를 들면 「일제강점기, 전투기를 몬 조선 여인이 있었다」 같은 느낌으로."**

11화부터 쓰던 **통념 부정(`X는 Y가 아니다`)을 훅 자리에서 내린다.**
그건 답을 미루는 구조라 첫 문장이 질문이 되는데, **이야기는 `~한 사람이 있었다`로 열어야
이야기로 들린다.** 가장 흥미로운 사실을 0~2초에 그냥 말한다.

| | 통념 부정 (11화~) | **선언 (12화~)** |
|---|---|---|
| 12화 | 조선 여자가 비행기를 배운 곳은 조선도 일본도 아니었다 | **일제강점기, 군용기를 몬 조선 여인이 있었다** |

⚠️ **통념 부정을 버리는 게 아니라 한 칸 뒤로 내린다.** 03번이 그것을 받아
첫 궁금증을 던지고 곧바로 답한다 — 12화 03번: "비행기를 배운 곳은 조선도 일본도
아니었다. 중국 윈난, 대륙의 남쪽 끝이었다." 훅이 선언, 03번이 해소라서 정보 공백이 없다.

⚠️ **06번은 이제 반전이 아니라 확인이 된다.** 그래도 된다 —
**숨기는 것은 이름 하나뿐**이고 마지막 호명이 여전히 유일한 공개다.

### ⚠️ 연표가 아니라 이야기로 쓴다 (2026-09-21 추가, 12화부터)

**사용자 지적 — "연도나 나이를 중요하다는 듯이 항상 언제 뭘 하고 몇 살에 뭘 했다고
나열하는 게 위주인데, 이력 전달만이 아닌 이야기처럼 말해 달라."**

맞는 지적이었다. 11~20화 전편이 **"N년에 A했다 / 몇 살에 B했다"의 연속**이었다.
12화는 387자에 **숫자·나이 표현이 열 개**였고 시간이 **여덟 번 튀었다.**

고치는 방법 셋:

1. **시간을 한 방향으로 편다.** 01·03이 결과를 보여준 뒤, 04번부터 그 사람의
   시작으로 내려가 끝까지 한 방향이다. 12화: 1925 → 1917 → 1919 → 1920 → 1927 → 1949 → 1988.
   **역행은 한 번(04번)뿐이다.**
2. **숫자를 줄이고, 남긴 숫자에는 일을 시킨다.** 12화는 열 개 → 네 개로 줄였고
   각각 **결심한 나이 / 기다린 시간 / 쌓은 양 / 늦은 귀향**을 맡는다.
   나이는 하나만 남기고 나머지는 **화면 연도 스탬프**에 맡긴다.
3. **요약을 장면으로 바꾼다.** "곡예비행을 보고 비행사가 되기로 했다"(요약) →
   "**하늘에서 재주를 넘는 것을 보았다. 그날 비행사가 되기로 했다**"(장면 + 결과).

⚠️ **이건 심리·정황을 지어내라는 뜻이 아니다.** §3의 금지 유형은 그대로다.
**사료에 있는 사실의 순서와 분량과 표현만 바꾼다.**

### ⚠️ 마무리를 한 공식으로 굳히지 않는다 (2026-09-21)

**사용자 지적 — "`~한 지 N 만이었다`로 무조건 끝내라고 지시한 적 없다."**
'감정을 넣어라'를 어미 하나로 고정한 것이 문제였다. 실제로 13~19화를 다 쓰고 보니
**여섯 편이 전부 같은 구문**이었고, 11·12화까지 세면 **아홉 편 중 여덟 편**이었다.

규칙 2의 본래 뜻은 **"두 날짜를 나란히 놓는다"**이지 특정 어미가 아니다.
12화는 계산된 간격을 버리고 **사실 둘만 놓았다** — "**평양에서 태어나 서울에 묻혔다**".
해석도 인과도 없고 감정은 시청자가 채운다.

⚠️ **회차를 하나씩 쓰면 안 보인다. 다 쓴 뒤 마지막 문장만 세로로 훑어야 보인다.**

### 감정은 지어내지 않고 배치로 만든다 (2026-09-20 추가)

사용자 지적 — "팩트 나열만 있는 것 같다". 맞는 지적이지만
**해법은 심리·정황을 쓰는 것이 아니다.** 그건 1화에서 사고가 난 방식이다
("아무도 그를 의심하지 않았다. 의심할 이유가 없었기 때문이다" ← 사료에 없음).
§3의 금지 유형(인과 창작·정황 묘사)은 그대로 유효하다.

**대신 같은 사실을 어디에 놓느냐로 만든다.** 이미 쓰고 있는 장치다:

| 회차 | 마지막 문장 | 재료 |
|---|---|---|
| 14 | 의병을 일으킨 지 일곱 달 만이었고, **그날은 새해 첫날이었다** | 순국일이 1월 1일 |
| 15 | 국적을 얻은 것은 2009년이었다. **숨을 거둔 지 일흔세 해 만이었다** | 1936 / 2009 |
| 20 | 서른하나였다. **나라가 사라지기 두 달 전이었다** | 순국 6월 / 병합 8월 |
| 11 | **고향 황해도를 떠난 지 두 해 만이었다** | 일본행 1926 / 순국 1928 |

규칙 셋:

1. **마지막은 그 사람으로 되돌린다.** 11화는 원래 "찔린 일본 대장은 이듬해 죽었다"로
   끝나 **조명하 이야기인데 가해자로 닫혔다.** 가해자·사건의 후일담은 앞 컷으로 보낸다.
2. **두 날짜를 나란히 놓는다.** 사료에 있는 숫자 둘을 병치하면 해석 없이도 무게가 생긴다.
   "~한 지 N년 만", "~하기 N달 전".
3. **앞에 이력을 깔아야 마지막 한 문장이 작동한다.** 11화의 "두 해"는
   07·07b(보통학교 → 일본 공장 → 오사카 → 대만)를 깔아둔 덕에 값을 한다.
   이력 없이 그 문장만 있으면 그냥 숫자다.

⚠️ **같은 숫자를 두 번 말하지 않는다.** 11화는 06번에 이미 '스물세 살'이 있어
08b에서 나이를 반복하지 않았다. 마지막 문장의 힘이 빠진다.
- **마무리 고정 포맷**: `오늘 우리가 부른 이름. OOO. 기억하겠습니다.`
  - 쉼표가 아니라 **마침표로 끊는다.** TTS는 쉼표를 "문장이 이어진다"로 읽어 끝음을 올린다.
  - "기억해주세요"(요청)가 아니라 **"기억하겠습니다"(다짐)**. `~세요`는 요청형이라
    본질적으로 끝이 올라가고, 의미상으로도 시청자에게 부탁하는 것보다 채널이
    스스로 약속하는 쪽이 유언에 응답하는 구조가 되어 묵직하게 닫힌다.
- **상단 소제목은 회차마다 바꾼다.** 채널명 고정보다 후킹이 낫다는 판단.

### ⚠️ 소제목 규칙 전면 개정 (2026-09-14, 3화부터 적용)

**예전 규칙**: 반전 정보(나이·직업·성별·국적)를 소제목에 절대 넣지 않는다.
**바뀐 규칙**: **숨기는 것은 이름 하나뿐이다. 반전 사실은 소제목에 다 내놓는다.**

사용자 판단 — "결국 소제목으로 이 영상을 볼지 안 볼지가 정해지는데, 임팩트를 다 뒤로
빼니까 첫 후킹이 부족하다." 실제로 초반 제목들은 "그는 ○○했다" 꼴이라 주어가 비어 평평했다.

**포맷은 안 깨진다.** 사실을 앞에 내놔도 이름은 여전히 마지막에만 나온다.
질문이 "무슨 일이 있었나"에서 **"이게 누구지"**로 바뀔 뿐이고, 그 답이 곧 마지막 호명이다.
오히려 중간(06번)의 사실 공개와 마지막 이름 공개가 서로 김을 빼던 문제가 없어진다.

쓰면서 걸러낸 것들:

- **요약하지 말고 장면을 남긴다.** "판사가 사형수가 됐다"(요약)보다
  "재판하던 사람이 재판을 받았다"(같은 말 뒤집기)가 낫다. 정확히 줄이려 들면 심심해진다.
- **절은 하나, 대비는 한 쌍.** 정보를 두세 개 나열하면 평평해진다.
  ("예순네 살 노인이 총독에게 폭탄을 던졌다" ❌ — 나이·대상·행위를 다 넣어 밋밋)
- **종결형을 `~했다`로 고정하지 않는다.** 명사형·헤드라인체가 더 붙을 때가 있다.
  ("폭탄을 나른 중국인 노파", "일본 헌병이 의병을 탈출시키다", "3·1운동을 촬영한 사람")
- **소제목이 일부러 오해시키고 본문이 푸는 구조**를 쓸 수 있다.
  7화는 "중국인 노파"라 해놓고 06번에서 "그는 중국인이 아니었다"로 뒤집고,
  8화는 "일본 헌병"이라 해놓고 위장 귀순이었음을 푼다.
  ⚠️ 이 구조는 **본문에 정정 문장이 반드시 있어야** 성립한다. 없으면 시청자가 잘못 기억한다.
- **화면 폭 한계 960px.** 소제목 54px 기준 대략 21자다. 넘으면 양옆 가는 선이 사라지고
  화면이 꽉 막힌다. 확정 전에 `archive_layout`으로 폭을 재 볼 것.
- **댓글 유도는 나레이션에서 뺀다.** 순국 이야기 직후 CTA는 톤을 깬다.
  대신 **고정 댓글**로 처리한다 → 엔게이지먼트와 톤을 둘 다 챙긴다.
  (고정 댓글 문구: "다음에 부르고 싶은 이름을 댓글로 남겨주세요.")

## 3. 검증 규칙 (실존 인물이므로 최우선)

- **나무위키 사용 금지.** CC BY-NC-SA 2.0 KR이라 NC(비영리) 조항 때문에
  수익창출 영상에 문장을 쓸 수 없다. 소재 발굴용으로만 쓰고 사실은 아래로 교차 검증.
- 신뢰 출처: **국가보훈부 공훈전자사료관**(최우선), 한국민족문화대백과사전,
  우리역사넷(국사편찬위), 독립기념관 한국독립운동정보시스템
- 인용문(유언·유시)은 **반드시 원문 확인 후** 사용. 1화에서 실제로 초안 문구가
  틀려서 수정한 이력 있음.
- 사망/부상 표현 주의. "쓰러졌다"는 사망으로 읽힐 수 있으므로 사료 표현(중경상)에 맞춘다.

### 필수 절차 — 문장 단위 검증표 (2026-09-09 확정)

**TTS를 뽑기 전에** 대본 전 문장을 표로 만들어 사료와 1:1 대조한다.
나중에 발견하면 음성·프레임을 전부 다시 만들어야 한다.

| 문장 | 근거 사료 | 원문 인용 | 판정 |

✅가 아닌 문장은 삭제하거나 확인된 사실만 남긴다. 1화를 이 방식으로 다시 훑었더니
이미 한 번 검수한 대본에서 **6건**이 더 나왔다. 반복해서 나오는 유형:

1. **인과 창작** — "~때문이다", "~라서 ~했다". 사료는 사실만 적고 이유는 안 적는다.
   (1화: "아무도 그를 의심하지 않았다. 의심할 이유가 없었기 때문이다" → 근거 없음)
2. **정황 묘사** — "거리를 걸어 다녔다", "즉시 봉쇄했다". 극적이지만 대개 창작이다.
3. **수치 어림** — "37명 부상, 2명 사망"을 "서른 명 남짓이 다쳤다"로 뭉개면
   사망자가 사라진다. 사료의 숫자를 그대로 쓴다.
4. **직접 인용** — 기억으로 쓰지 말고 매번 원문을 다시 찾는다.
   (1화 유언 끝: "내가 소원하는 일이다" ❌ → "무엇보다 중요한 일이다" ✅)
5. **나이** — 만/세는나이, 사건 시점/순국 시점이 섞인다. 생년월일에서 직접 계산한다.
   (1화: 의거일 만 64세인데 순국 시 나이 65를 썼다)
6. **"평생 ~한 사람" 요약** — 다른 이력이 있으면 거짓이 된다.
   (강우규는 한약방만 한 게 아니라 만주에 광동학교를 세우고 교장을 했다)
7. **소제목도 검증 대상** — 1화 제목이 창작 문장 그대로였다.

### 시간 공백 점검

"만주에 학교를 세웠다" 다음에 곧바로 "다시 만주로 향했다"가 오면 그 사이가 비어
이상하게 읽힌다. 세그먼트를 연도순으로 늘어놓고 **장소가 바뀌는 지점**에 사료 근거가
있는지 확인한다. (2화: 백과사전의 "상해에서 대련으로 향하던 도중"을 써서 메웠다)

## 4. 인물 선정 — 제외 기준

하나라도 걸리면 제외:
1. 친일 행적(변절, 친일단체, 학병 권유 등)
2. 친일인명사전 / 친일반민족행위 명단 등재
3. 서훈 취소 또는 취소 논란 진행 중
4. 좌우 이념 대립으로 국내 정치 논쟁이 큰 인물 (예: 홍범도, 김원봉, 여운형)
5. 유족 분쟁이나 행적 진위 논란

> ⚠️ **이 목록은 '주인공으로 삼지 않는다'는 뜻이고, 단체명까지 지우라는 뜻이 아니다**
> (2026-09-14 정정). 한동안 김원봉을 피한다는 이유로 **의열단**까지 대본에서 뺐는데
> 근거가 약했다. 의열단은 공훈록·백과사전이 정상 서술하는 공식 독립운동 단체이고
> 단원 다수가 서훈을 받았다 — 6화 김익상 본인이 의열단원이자 건국훈장 대통령장이다.
> 논쟁 대상은 **김원봉 개인**의 월북·서훈 문제다. 단체명을 뭉개면 행적의 실체가 사라진다
> (10화 이육사는 의열단 가입이 독립운동 행적의 출발점이다).

## 5. 이미지 규칙

- **실존 인물의 얼굴을 AI로 생성하지 않는다.** 허위정보 + 유족 반발 + 유튜브
  AI 콘텐츠 라벨 이슈가 겹친다.
- 인물 얼굴은 **퍼블릭 도메인 / 공공누리(KOGL)** 실사진만. (1화 강우규 초상은
  위키미디어 커먼즈 PD — 1920년 이전 촬영, 저작권 만료 확인)
- AI 생성은 **시대 배경·장소·상징물**까지만. 사람이 나올 땐 뒷모습·원경·실루엣으로
  얼굴이 식별되지 않게 한다.
- 전 컷에 동일한 세피아 그레이딩을 씌워 실사진과 AI 이미지의 질감을 통일한다.

### 초상 확보 절차 (2026-09-10 확정)

**대본보다 먼저 초상을 확보한다.** 못 구하면 마지막 컷 구성이 통째로 바뀐다.

> ⚠️ **"미확보"는 찾아본 뒤에만 쓸 수 있는 말이다 (2026-09-21).**
> 11화 조명하 검증표에 "미확보. 2020년 대만에서 사진이 발견됐다는 언론 보도가 있으나
> 라이선스가 확인되지 않는다"고 적어 놓고 **아래 1번(커먼즈 API 조회)을 건너뛰었다.**
> 검색 결과의 기사 제목만 보고 단정한 것이다. 실제로 조회하니 **PD 파일이 바로 나왔다.**
> 12~20화도 "전편 미확보"로 적었는데 조회하니 **8편 중 7편에 후보가 있었다.**
> → 조회하지 않았으면 "**확인하지 않음**"이라고 적는다. 검증표는 *확인한 것*의 기록이다.

1. **위키미디어 커먼즈 API로 라이선스를 직접 확인한다.** 검색 결과 화면이 아니라
   메타데이터를 본다:
   ```
   commons.wikimedia.org/w/api.php?action=query&titles=File:<파일명>
     &prop=imageinfo&iiprop=url|extmetadata&format=json
   ```
   `LicenseShortName`이 **Public domain**인 것만 쓴다.
2. **같은 인물에 PD와 CC가 섞여 있다.** 이회영은 `Lee Hoe-yeong.jpg`가 PD(1920년대)인데
   `Yi Heoyeong.jpg`는 **2021년 제작물이라 CC BY-SA**다. 이름만 보고 받으면 안 된다.
3. **저해상도 크롭을 받지 말고 원본에서 직접 자른다.** 이회영 초상을 한동안
   142×188짜리로 쓰고 있었는데, 커먼즈 원본이 1024×1408 전신 사진이었다.
   원본을 받아 상반신을 잘라 쓰면 화질이 크게 올라간다.
4. **동명이인을 반드시 걸러낸다.** 인터넷에 도는 '안경신' 증명사진은 취재 결과
   원불교인 안경신(1885~1975)이었다. 실제 초상은 조선일보 1927년 12월 17일 5면 사진이다.
5. **공훈전자사료관 이미지는 함부로 쓰지 않는다.** 사진이 아니라 현대 화가가 그린
   초상화인 경우가 많고, 페이지에
   `Copyright(c) By Ministry of Patriots & Veterans Affairs All Rights Reserved`가 붙어 있다.
   공공누리 표시가 명시된 것만 쓴다.

**초상을 못 구했을 때 — 빈 인화지 + 표기 (2026-09-11 표준 확정)**

`assets/photos/blank_plate.png`(누렇게 바랜 빈 인화지, PIL로 생성)를 마지막 컷에 놓고
이름표를 얹은 뒤, **크레딧 첫 줄에 사유를 적는다.**

```json
"credit": [
  "전하는 사진이 확인되지 않아 빈 인화지로 둡니다",
  "자료 · ..."
]
```

빈 화면만 두면 시청자가 의도인지 사고인지 알 수 없다. 표기가 있어야
빈 공간이 실수가 아니라 **사실 자체**로 읽힌다.

> ⚠️ **문구는 반드시 "확인되지 않아"다.**
> "남아 있지 않습니다" / "전하는 사진이 없습니다"는 **검증하지 않은 주장**이다.
> 확인한 것은 '커먼즈 0건 · 위키백과 없음 · 공훈록은 몽타주'까지이지
> '현존하지 않는다'가 아니다. 대본(나레이션)에서는 아예 언급하지 않는다.

배치는 **이름표 아래 출처 자리**다. 인화지 안에 글자를 넣는 안도 만들어 봤지만
인화지라는 설정이 깨져서 택하지 않았다.

**4화 허위 사례**: 공훈전자사료관에 그림이 있으나 대경일보(2018-10-22)에 따르면
**증손녀 허미라 씨가 생김새 설명만 듣고 그린 몽타주**다. 두 가지 이유로 쓰지 않았다 —
(1) 현존 작가의 미술저작물이고 페이지에 공공누리 없이 `All Rights Reserved`만 있다.
수익창출 영상에서 전체를 주인공으로 쓰는 건 인용·공정이용 범위를 벗어난다.
(2) 실제 얼굴이 아니다. 추정화를 이름표와 함께 초상처럼 배치하면 시청자는 실물로
받아들인다 — 'AI로 실존 인물 얼굴을 만들지 않는다'와 같은 취지에서 어긋난다.

**12화 권기옥 (2026-09-21 확보)**: `File:Kwon-Ki-ok-1.jpg` — 1606×2234, `LicenseShortName: Public domain`,
`Copyrighted: False`, Unknown photographer, 1925~1939. 비행 헬멧·고글 차림 상반신.
620×862(비율 1.39)로 **축소만 하고 크롭하지 않았다**(가장자리 열·행 평균 130~194로 잘라낼 검은 띠가 없었다).
동일인 검증은 **Wikidata Q489782**로 했다 — 두 후보 파일이 모두 이 항목에 걸려 있고 생몰이
1901-01-11 / 1988-04-19로 대본과 일치한다. 워터마크는 축소본이 아니라 **원본 해상도에서** 확인했다.
탈락: `Kwon Ki-ok1928.jpg`도 PD지만 555×786이고 배경 광고 포스터의 영문이 읽힌다.

확보 현황: 강우규 PD · 이회영 PD · 안경신 PD(조선일보 1927) · 가네코 후미코 PD(c.1925) ·
권기옥 PD(c.1925~39) ·
**허위 없음 → 빈 인화지**

## 6. 비주얼 — 아카이브 레이아웃

양산형 숏츠의 시그니처(**상하단 두꺼운 검은 바 + 가운데 정렬 흰 외곽선 자막**)를
쓰지 않는 것이 핵심.

- 배경: 낡은 종이 질감 (단색 검정 금지). 큰 얼룩 + 미세한 결 2층 구조
- 사진: **찢긴 가장자리**의 인화지. 진폭 11px로 작게, 대신 고주파로 잘게 떨리게
  (진폭을 키우면 톱니처럼 보여 오히려 가짜 티가 난다)
- 노후화: 폭싱 반점(작게, 군집), 얼룩진 노출, 가장자리 변색, 미세 스크래치, 선예도 손실
- **광량 누출** — 사진 색을 배경으로 번지게 해 사진이 배경에 얹힌 게 아니라
  배어나온 것처럼 만든다. 암부는 배경 먹색과 일치시킨다
- 자막: **좌측 정렬**. 연도 스탬프는 좌상단에 작게, 소제목은 상단 중앙
- 세이프 영역: **상단 90** / **하단 430** / 우측 170.
  하단은 처음 320으로 잡았다가 숏츠 UI(제목 2줄+채널명+버튼+진행바)에 걸려 430으로 올렸다.
  상단은 150에서 90으로 내렸다 — 아래 경고 참조.

### 확정 세로 배치 (2026-09-11 전면 재조정)

| 요소 | y | 비고 |
|---|---|---|
| 상단 안전선 `TOP_SAFE` | **90** | 아래 경고 참조 |
| 소제목 `TITLE_SIZE` 54px | **90** | 잉크는 106부터 시작(bbox 상단 여백 16px) |
| 연도 `YEAR_Y` | **258** | 밑줄 322 |
| 사진 `PHOTO_TOP` | **370** | 카드 아래 1022, 줌 끝 1042 |
| 자막 경계 `CAPTION_TOP` | **1060** | 넘으면 장을 나눈다 |
| 자막 기준선 `CAPTION_BOTTOM` | 1380 | |

> ⚠️ **`TOP_SAFE=150`은 근거 없는 값이었다.** 잡학쿠키에서 가져왔는데 거기는
> 상단에 검은 바가 깔린 레이아웃이고, 150은 그 바 안에서 타이틀을 밀어내는
> 여백이었다. 이 레이아웃의 안전선으로 검증한 적이 없었다. 사용자가 "노치가
> 저렇게 밑에까지 있냐"고 물어서 드러났다. 숏츠에서 상단을 가리는 것은 거의
> 없고(간섭은 우측 버튼 컬럼), 요즘 폰은 화면비가 길어 9:16 영상이 상하 여백을
> 두고 들어간다. y=60·90에 타이틀을 얹어 보여드리고 **90으로 확정**.
> **아직 실제 숏츠 플레이어에서 검증하지 않았다.** 첫 업로드 때 상단이 잘리는지 확인할 것.

> ⚠️ **`YEAR_Y`를 `TOP_SAFE`에 묶지 말 것.** 예전에 `TOP_SAFE + 78`이었는데
> 안전선을 내리자 연도까지 딸려 올라가 사진과 벌어졌다. 연도는 사진에 붙어야 한다.

> ⚠️ **`PHOTO_TOP=370`이 한계다.** 켄번스 6% 줌이 카드를 아래로 20px 더 밀어
> 380부터는 자막 영역을 침범한다.

**소제목은 항상 `TOP_SAFE`에 붙인다.** 크기를 바꾸려면 `set_title_size(size)` 한 번이면
되고, 양옆 가는 선은 **크기에 반비례**해 짧아진다(`100 * (38/size)^1.5`). 글자가
무거워지면 선은 장식이 아니라 작은 티크로 물러나야 균형이 맞다. 양옆에 자리가
없으면 선을 아예 그리지 않는다.

세로 사진 상한 `MAX_PHOTO_H = 600` — 아래 이름표(2줄)와 자막이 들어갈 자리를 확보해야 한다.

### 폰트
- **Noto Serif KR, weight 400** (`assets/fonts/NotoSerifKR-var.ttf`, SIL OFL — 상업적 이용 자유)
- 자막 46px, 연도 스탬프 48px(weight 500)
- 시스템에 애플명조뿐이라 별도로 받아 씀

### 줄바꿈
**구두점 기준 절 단위 분할.** 구두점 뒤 몇 글자만 걸치고 줄이 바뀌는 걸 막는다.
절이 통째로 안 들어가면 구두점에서 미리 끊고, 절 하나가 한 줄을 넘을 때만 어절 단위로 쪼갠다.

### 자막이 넘치면 장을 나눈다 (2026-09-11 변경)

자막은 `CAPTION_BOTTOM`(1380)을 기준으로 위로 자란다. 길면 사진을 덮는다.

한동안 **글자 크기를 줄여** 해결했는데, 그러면 그 컷만 글자가 작아져 회차 안에서
자막 크기가 들쭉날쭉해진다. 사용자 지적대로 **책장 넘기듯 다음 장으로 넘기는 게 맞다.**

`archive_layout.caption_pages(text, size, weight)`가 줄바꿈과 분장(分張)을 한 번에 한다.
- 한 장 최대 줄 수 = `(1380 - 1060) / (size * 1.42)` → 46px에서 **4줄**
- **고르게 나눈다**: 5줄이면 4+1이 아니라 **3+2**. 마지막 장에 한 줄만 남으면 허전하다.
- **장마다 top을 고정**한다. 아래 기준으로 앉히면 줄 수가 다른 장끼리 블록이 튄다.
- `page_at(counts, reveal)`이 지금 찍힌 글자 수로 몇 번째 장의 어디인지 돌려준다.

로그에 `자막 5줄 → 2장으로 나눔 (3+2줄)`처럼 찍힌다. 장이 자주 나뉘면 문장을
짧게 고치는 편이 낫다 — 한 컷에 두 장이 넘어가면 읽는 속도가 급해진다.

## 7. 모션

- **자막 타이핑**: 세그먼트 0프레임부터 **글자 수에 비례해** 찍는다.
  전환이 끝난 뒤 시작하면 음성보다 늦어 어긋나 보인다. TYPE_RATIO 0.88
- **컷 전환 = 사진 얹기(drop)**: 새 사진이 살짝 기울어진 채 위에서 내려와 덮고,
  이전 화면은 30% 지점부터 스르륵 걷힌다. 32프레임(약 1.07초), smoothstep 이징.
  (ease-out은 초반이 급해 "후다닥" 느낌이 난다. 책장 넘김은 컨셉 불일치로 폐기)
- **느린 줌**: 찢긴 테두리까지 **인화지 전체가 통째로** 커진다. 6%.
  확대 후 원래 크기로 되잘라내면 찢긴 결이 깎여나가므로 잘라내지 않는다
- **전환은 시간을 추가로 쓰지 않는다.** 세그먼트 길이 안쪽에 포함시킨다.
  사이에 끼워 넣으면 전환 8회 × 1초가 통째로 늘어 60초를 넘긴다

### 장면 효과 — 매 회차 기본 적용 (2026-09-14 확정)

**대본 JSON을 만들 때 세그먼트마다 `fx`를 채운다.** 비워 두면 1화만 살아 있고
나머지는 정지 사진처럼 보인다(2·3화가 실제로 그랬다). 이미지 내용으로 고른다:

| `fx` | 쓰는 곳 |
|---|---|
| `smoke` / `smoke_soft` | 폭발 직후, 산안개, 연기 나는 장면 |
| `dust` | 야외·행군·문서·군중 — 기본값에 가깝다 |
| `flicker` / `flicker_soft` | 감옥·법정·실내 조명, 초상 직전 컷 |
| 없음 | `09_cta` 추모 컷. **`motion: false`도 같이 준다** |

**`impact`(효과음 + 화면 흔들림)는 조건부다.** 대본에 **폭발·총성이 실제로
일어나는 문장**이 있을 때만 그 세그먼트에 넣는다. `{"sound": "explosion"|"gunshot",
"at_sentence": n, "db": -6, "shake": 18}` 형태로, 사건을 말하는 문장 끝에 얹는다.

- **회차당 한 번.** 흔하면 순국을 다루는 채널이 액션 편집처럼 읽힌다.
- **추모 컷·인용 컷에는 절대 넣지 않는다.**
- 계획만 있고 터지지 않은 사건에는 넣지 않는다.
  (5화 가네코는 "일왕을 죽이려 했다"가 미수라 무음, 4화 허위는 교수형·체포뿐이라 무음)
- 효과음은 `impact_fx.py`에서 **직접 합성한다.** 받아 쓰면 Content ID가 걸린다.

### 업로드 정보는 대본 JSON에서 만든다 (2026-09-14 확정)

`scripts/upload_info.py`가 제목·설명란·태그·고정 댓글을 **대본 JSON 하나에서** 만든다.
대본집 페이지(`build_archive_page.py`)와 `drafts/upload_*.md`가 같은 함수를 쓰므로
두 곳이 어긋나지 않는다. 회차 JSON에 `upload` 필드만 채우면 된다:

```json
"upload": {"tags": [...], "playlists": [...], "hashtags_extra": [...]}
```

- **⛔ 설명란에 URL을 쓰지 않는다** (사용자 지시). 출처는 기관명·자료명까지만 남긴다.
  근거를 밝히는 목적은 그것으로 충족되고, 링크가 길면 설명란 앞줄이 밀려 접힌다.
  전체 URL은 JSON의 `sources`와 대본집 '검증 기록'에 그대로 있다.
- **해시태그 순서 고정.** 유튜브는 설명란 앞 3개를 제목 위로 올린다.
  `#독립운동가 #한국사 #근현대사`가 앞이고 **인물 이름은 반드시 4번째 이후**다.
  이름에 공백이 있으면 거기서 잘리므로 붙여 쓴다(`#프랭크스코필드`).
- 초상 미확보 회차는 설명란에도 "전하는 사진이 확인되지 않아 빈 인화지로 두었습니다"를 적는다.

### 배경 이미지 — `scripts/gen_images.py`

45장짜리 장면 사전을 들고 `codex exec`(ChatGPT 구독 쿼터)로 뽑는다.
**쿼터가 실재한다** — 2026-09-14에 24장째에서 막혔고 해제까지 약 4시간 30분 걸렸다.
스크립트는 이미 있는 파일을 건너뛰고 실패하면 멈추므로, 해제 뒤 같은 명령을
다시 돌리면 이어서 채운다. `.env`의 `OPENAI_API_KEY`는 비어 있어 유료 폴백은 없다.

## 8. 오디오

- **나레이션**: Typecast `ssfm-v30`, voice **Piljae**, 최상위 `speed = 0.95`
  - 본문: `prompt.emotion_preset = "tonedown"`
  - **마무리 3문장: `sad`, `emotion_intensity = 1.4`**
  - `speed`와 `emotion_intensity`는 **최상위/prompt 위치가 다르다.**
    `speed`를 `prompt` 안에 넣으면 422(VALIDATION_ERROR)가 난다.
- 1인 내레이션. 잡학쿠키의 남녀 2인 대화는 이 소재에 쓰지 않는다
- **배속은 대본별로 정한다** — 대본 JSON의 `playback_speed`. 없으면 1.15.
  - 1~10화: **1.15** (확정. 건드리지 않는다)
  - 11화~: **1.25** (2026-09-20 변경, 유지율 개선)
  - ⚠️ 전역 상수 하나로 두지 말 것. 새 배속을 시험하면 **이미 낸 회차까지 같이 바뀐다.**
  - ⚠️ **상한은 1.25로 본다.** 이 채널은 순국·고문·처형을 다룬다. 잡학쿠키(1.3~1.55)와
    같은 속도로 읽으면 톤이 경박해진다 — 음슴체를 금지한 것과 같은 이유다.
- **정적도 대본별로 정한다** — `silence_sec`(반전 직전) / `pause_after`(컷 뒤).
  - 1~10화: 반전 직전 **0.5초**, 이름 뒤 **1.2~1.4초**
  - 11화~: 반전 직전 **0.4초**, 이름 뒤 **1.0초**
    (배속 계산값은 1.1인데 들어보고 한 번 더 내렸다)
  - ⚠️ **배속을 바꾸면 정적도 같이 나눈다. 둘은 한 벌이다.** 11화에서 배속만 1.25로
    올리고 정적을 그대로 뒀더니 "이름 뒤 텀이 길다"는 지적을 받았다 — 말이 8% 빨라지면
    같은 1.4초도 그만큼 길게 들린다. 1.4÷1.25≈1.1, 0.5÷1.25=0.4로 맞췄다.
  - ⚠️ **채널명 뒤 1.4초는 없앴다**(2026-09-18). 7~8초 이탈의 원인이었다 — §2 참고.

### ⚠️ TTS는 비결정적이다
같은 텍스트·파라미터로 5회 호출한 결과가 **길이 1.10~1.70초, 레벨 3dB** 차이가 났다.
억양도 매번 다르다. 따라서:
- **마음에 든 테이크는 `output/audio/locked_takes/`에 보존한다. 재생성으로 복원 불가.**
- 대본 JSON의 `locked_audio` 필드로 해당 세그먼트를 지정하면 파이프라인이
  그 파일을 쓰고 **배속을 건너뛴다**(사이까지 계산해 조립한 것이라 배속을 다시 걸면 깨진다).
- A/B 비교를 할 때는 "설정 차이"와 "테이크 차이"가 섞이므로, 여러 테이크를 뽑아
  그중 고르는 방식으로 진행해야 한다.

### ⚠️ 레벨 정규화 필수
감정 프리셋마다 출력 레벨이 크게 다르다 — `tonedown` -39dB, `sad 2.0` -20dB.
같은 프리셋 안에서도 문장마다 편차가 있다(본편 실측 -28~-35dB, 7.3dB 폭).
`build_episode.py`가 **세그먼트별로 평균 -26dB에 맞춘 뒤** 이어붙인다(편차 0.3dB).
이걸 빼면 장면이 바뀔 때마다 목소리 크기가 튄다.

### 마무리 조립 구조 (2026-09-10 확정)
```
[0.5s 사이] → "오늘 우리가 부른 이름." → [0.70s 사이] → 이름 → [0.75s 사이] → "기억하겠습니다."
```
- 문장을 **따로 생성해 이어붙인다.** 한 번에 읽히면 억양이 이어져 끝음이 올라간다.
- 앞뒤 두 문장(`common_A_head.wav` / `common_C_tail.wav`)은 **모든 회차가 공유**한다.
  회차마다 새로 뽑으면 TTS 비결정성 때문에 마무리 톤이 편마다 달라진다.
- 자막은 사이 구간에 멈춰야 하므로 `reveal_points`([진행비율, 누적글자수])로 제어한다.
  마지막 지점은 **클립 끝이 아니라 발화 종료 시각**에 맞춘다.

#### 이름은 캐리어 문장에서 잘라낸다 — 단독 합성 금지

이름만 단독으로 합성하면 TTS가 **호명하듯** 읽어 끝음이 뜬다. 본문 06번
("…그의 이름은, ○○○.")과 억양이 달라 보인다는 지적을 받았다.

`scripts/name_from_carrier.py`가 **「그의 이름은, ○○○.」을 본문과 같은 설정
(tonedown, 배속 1.15)으로 합성한 뒤 쉼표 뒤 이름만 잘라낸다.** 06번과 같은 조건이라
문미 하강 억양이 그대로 붙는다.

**⚠️ 경계 탐지의 임계값(gap 50ms / rel 0.06)을 절대 풀지 말 것.**
한때 "못 찾으면 임계값을 낮춰 재시도"하도록 짰다가 **이름 음절 사이의 약한 지점을
쉼표로 착각해 이름이 잘려나갔다**(강우규 1.83s, 이회영 1.21s 지점에서 절단).
느슨하게 볼수록 이름 안에서 끊길 위험만 커진다. 이 기준에서 정상 테이크는
묵음 구간이 **쉼표 자리 하나만** 나온다. 2개 이상이거나 이름 길이가
음절당 0.20~0.55초를 벗어나면 **기준을 푸는 게 아니라 다시 합성한다**(최대 5회).

#### 꼬리의 숨소리 제거

「기억하겠습니다」 뒤에 "하…" 하는 날숨이 붙어 끝이 지저분했다. 잘라내면 "다"가
뭉툭해지므로 **페이드아웃으로 지운다** — 750ms부터 140ms에 걸쳐 900ms에 닫는다
(발화는 730ms에 끝나고 740~830ms가 날숨, 840ms부터 무음).
교체 전 원본은 `locked_takes/common_C_tail_ORIG_sad.wav.bak`에 남겨뒀다.

### 최종 믹스
- `amix`는 **`normalize=0`** 필수 — 기본값은 입력 개수만큼 볼륨을 나눠버린다
- `loudnorm=I=-14:TP=-1.5:LRA=11` (유튜브 기준), 이후 `-ar 48000`으로 되돌린다
  (loudnorm이 96kHz로 올려놓는다), 스테레오 출력
- **BGM은 직접 합성** (`scripts/make_bgm.py`). 남의 음원은 수익창출 시
  저작권 클레임 위험이 있고 합성물은 그 위험이 0이다.
  낮은 드론 + 드문드문 떨어지는 단음(볼륨 0.18) + 이름 불리는 지점에 스팅

## 9. 길이

**60초 미만 필수** (Shorts 탭에 잡히려면). **1화 확정 59.16초 — 여유 0.84초뿐이다.**
사이를 더 넣으려면 다른 구간을 줄여 상쇄해야 한다.

### ⚠️ 60초를 다 쓴다 (2026-09-21 사용자 지시, 12화부터)

> **"앞으로 만들 때는 60초 이내에 충분한 내용으로 풍부한 이야기 전달이 되도록 한다."**

**짧은 것은 미덕이 아니다.** 11화 유지율 개선 작업에서 '첫 보상을 앞당긴다'를 하다가
**분량 자체를 줄이는 쪽으로 흘렀고**, 그 결과 12~20화가 이력만 나열하는 연표가 됐다.
줄여야 할 것은 **공백과 중복**이지 내용이 아니다.

| | |
|---|---|
| **목표 분량** | **450~480자 / 52~56초** |
| 상한 | 500자 · 57초 (60초까지 3초 여유를 남긴다) |
| 환산 | **실제 초 ≈ 나레이션 글자수 × 0.117** (1.25배속·로고·정적 포함 실측값) |

⚠️ **분량이 남으면 검증표의 `쓰지_않은_것`을 다시 본다.** 거기엔 두 종류가 섞여 있다 —
**분량 때문에 뺀 것은 되살리고, 사료 때문에 뺀 것은 그대로 둔다**(§3).
실제로 18화는 1921년 상하이 탈출에서 1944년 죽음까지 **스물세 해가 통째로 비어 있었는데**,
분량으로 뺐던 '미국 유학 아홉 해'를 되살리니 그 구멍이 메워졌다.

⚠️ **사료가 짧으면 억지로 채우지 않는다.** 20화 안규홍은 공적조서 자체가 짧아 46초에서 멈춘다.
**없는 것을 지어내느니 짧은 편이 낫다**(§3).

## 9-1. 모바일 확인용 압축본

완성본이 10MB대라 전송이 자주 타임아웃된다. `output/preview/`에 압축본을 만든다.
```
ffmpeg -i 최종본.mp4 -c:v libx264 -crf 30 -preset slow -c:a aac -b:a 96k 압축본.mp4
```
**해상도는 1080×1920 그대로 유지한다** — 폰에서 확인하는 목적이 자막이 숏츠 UI에
걸리는지 보는 것이므로 해상도를 줄이면 판단이 무의미해진다. (10MB → 2.5MB)

## 10. 파이프라인 스크립트

**한 편을 만드는 차례** — 위에서 아래로. 각 단계는 앞 단계가 끝나야 의미가 있다.

| # | 명령 | 하는 일 |
|---|---|---|
| 1 | (사람) | 대본 JSON 작성 → `verified`에 문장별 근거. **여기서 막히면 제작에 들어가지 않는다** |
| 2 | `gen_images.py <접두어>` | 배경 9~10장. `SCENES`에 프롬프트를 먼저 넣어야 한다. 쿼터에 걸리면 거기서 멈춘다 |
| 3 | `generate_tts.py <대본> <audio_dir> [세그먼트id,…]` | 나레이션. **세 번째 인자로 고친 문장만 다시 뽑는다**(§8 비결정성) |
| 4 | `make_ending.py <이름> --slug epNN_xx --apply <대본>` | 마무리 컷. 이름을 캐리어 문장에서 잘라 조립 |
| 5 | `build_episode.py <대본>` | 오디오 조립 + 프레임 + 먹싱 → `output/최종본/` |
| 6 | `upload_info.py`(import) | 업로드 양식 → `drafts/upload_*.md` |
| 7 | `build_archive_page.py` | 기록집 HTML → **같은 Artifact URL로 재배포**([[reference-ireum-page]]) |

**부품 스크립트** (직접 부르지 않고 위에서 불린다)

| 파일 | 역할 |
|---|---|
| `archive_layout.py` | 종이 배경, 찢긴 카드, 노후화, 자막, 이름표 |
| `render_archive_video.py` | 낙하 전환, 느린 줌, 이징 |
| `live_card.py` | 사진 **안에서** 카메라가 팬·줌 (§7) |
| `impact_fx.py` | 폭발·총성 합성 + 화면 흔들림. **회차당 1회** |
| `make_bgm.py` | BGM 합성 (드론 + 단음 + 반전 스팅) |
| `make_logo.py` | 0.8초 사운드 로고 (§2 브랜드 컷) |
| `name_from_carrier.py` | 캐리어 문장에서 이름만 잘라내기 |
| `upload_info.py` | 제목·설명란·태그 생성 (대본 JSON이 단일 출처) |
| `build_archive_page.py` | 대본 + 검증 기록 → 기록집 HTML |

**한 번만 쓰는 것**: `make_channel_art.py`, `make_channel_banner.py`,
`make_playlist_covers.py`, `bgm_variants.py`, `title_variants.py`,
`motion_demo.py`, `transition_demo.py`, `pitch_fall.py`, `trim_tail.py`

⚠️ `upload_youtube.py`는 **없다.** 자동 업로드는 보류 상태다(§12).

### 제작 체크리스트 (11화에서 확립, 12화부터 적용)

- [ ] 대본 `verified`에 `제작_전_확인` 항목이 남아 있지 않은가 → **남아 있으면 먼저 해결**
- [ ] 훅이 **통념 부정**인가, 반전 축이 훅과 호응하는가 (§2)
- [ ] 소제목과 훅이 **같은 축을 두 번 치지 않는가**
- [ ] 마지막 문장이 **그 사람으로 돌아오는가** (가해자·후일담으로 끝나지 않는가)
- [ ] 한 대상을 **두 호칭으로 부르며 잇는 문장이 없는 곳**은 없는가 (11화 '장인/대장' 사고)
- [ ] 나이가 **만나이**인가 (사료의 세는나이를 그대로 옮기지 않았는가)
- [ ] `02_brand`가 `kind:"logo"` + `logo_bell.wav`인가, `pause_after`에 `02_brand`가 없는가
- [ ] `playback_speed`가 있는가 (11화~ **1.25**)
- [ ] 엔딩 해시가 다른 회차와 겹치지 않는가 → `make_ending.py`가 자동으로 막는다
- [ ] **초상을 커먼즈 API로 실제 조회했는가** — 안 했으면 '미확보'라 쓰지 않는다(§5)
- [ ] 빈 인화지면 `credit` 첫 줄에 사유 표기가 있는가 → `build_episode.py`가 자동으로 막는다
- [ ] 초상을 쓴다면 **동일인 검증**(파일 사용처)과 **워터마크 확인**을 했는가
- [ ] 렌더 후 `output/video/`의 프레임 잔여물을 지웠는가 (편당 2GB, §14)

## 11. 연간 운영 계획

365편을 인물 단독편으로만 채우면 3~4개월에 소재가 마른다. 비율:

| 포맷 | 연간 | 비고 |
|---|---|---|
| 인물 단독편 | ~240 | 메인 |
| 사건 중심편 | ~50 | 봉오동·청산리·의열단 의거 등 |
| 묶음편 | ~40 | "10대 독립운동가 3인" 식 |
| 유물·장소편 | ~35 | 서대문형무소, 안중근 유묵 |

**인물 등급** — S(반전 최상, 월 2~3편) / A(주력 50%) / B(검증 쉬움, 제작 절반, 30%)
S급 사이에 B급을 끼워 제작 부하가 몰리지 않게 배치한다.

**업로드**: 초기 매일. 단 **런칭 전 14편 재고 확보 + 주 1회 배치 제작**.
빈도를 낮추는 대신 재고로 버틴다. 40편쯤 쌓여 유형별 성과가 보이면 그때 조정.

**업로드 시각: 매일 21:00 (잠정, 2026-09-14 결정).**
피크 시간에 올리는 게 아니라 **피크가 시작되기 직전에 올린다.** 숏츠는 업로드 직후
소규모 표본에 뿌려 반응을 보는데, 이 초기 테스트 구간이 21~24시 피크와 통째로
겹쳐야 신호가 빨리 모인다. 22시대에 올리면 피크의 뒷자락만 먹는다.
20~21시는 사람이 깨어 있어도 설거지·이동·TV 시간이라 스크롤 밀도가 낮아 20:00은
차선(21:00을 못 지키는 날에만 20:30으로 당기고, 22시로 미루지 않는다).
아침 출근길은 트래픽은 크지만 순국·고문 소재의 완주율이 떨어져 뺐다.
**시각을 흔들지 않는 것이 시각 자체보다 중요하다** — 숏츠는 롱폼보다 업로드 시각의
영향이 작고, 대신 "이 채널은 밤 9시"라는 시청자 습관이 남는다.
편수가 20~30편 쌓이면 추정이 아니라 **분석 → 시청자 → '시청자가 YouTube를 이용하는
시간대'** 실측으로 옮긴다.

**월별 테마**: 3월 3·1운동 / 4월 임시정부 / 6월 현충일 / 8월 광복절 / 11월 순국선열의 날

## 12. 진행 상황 (2026-09-21 기준)

### 지금 상태

| | |
|---|---|
| 대본 | **20편** (`drafts/script_dok_*.json`) |
| 완성본 | **12편** (1~12화) |
| 업로드 | 1~10화. **11·12화는 아직 안 올렸다** |
| 포맷 | **11화부터 개편** — 아래 참고 |

### 11화에서 바뀐 것 (12화부터 그대로 적용)

유튜브 유지율에서 **7~8초에 30% 이탈**이 확인되어 포맷을 손봤다.
원인은 훅이 약해서가 아니라 **훅 직후의 3.9초 정보 공백**이었다(§2).

| 항목 | 1~10화 | 11화~ |
|---|---|---|
| 브랜드 컷 | 나레이션 2.5초 + 무음 1.4초 | **0.8초 사운드 로고** + 화면 자막 |
| 훅 | 사건 서술 | **통념 부정** (`X는 Y가 아니다`) |
| 배속 | 1.15 | **1.25** (`playback_speed`) |
| 반전 직전 정적 | 0.5초 | **0.4초** (`silence_sec`) |
| 이름 뒤 정적 | 1.2~1.4초 | **1.0초** |
| 마지막 문장 | — | **그 사람으로 되돌린다** (§2 감정 배치) |
| 재생목록 | 2개 | **'전부 부른 이름' 하나** |
| 제목 | 소제목만 | 소제목 + `#독립운동가` |
| 설명 첫 줄 | — | 연도·지역·사건 키워드(`upload.lede`) |

**11화 조명하**: **46.0초**, 375자, 8초 시점이 본문, 첫 보상 16.3초, **초상 확보**(커먼즈 PD)
(1~10화는 첫 보상이 25초 전후였다).

⚠️ **1~10화는 다시 만들지 않는다.** 재업로드하면 쌓인 조회수와 알고리즘 신호가 리셋된다.
11화 유지율을 1~10화와 비교해 개편 효과를 판정한 뒤 다음을 정한다.

### 12~20화 — v2 전환 완료 (2026-09-21)

**아홉 편 전부 개편 포맷으로 다시 짰다.** 12화는 제작까지 마쳤고, 13~20화는 대본·초상까지 끝냈다.

| | 12~20화 전환 결과 |
|---|---|
| 컷 | 10~11 → **11~12** |
| 분량 | 348~448자 → **359~422자**(43~50초) |
| 로고 컷 | 전편 `kind:"logo"` + `logo_bell.wav`, `pause_after`의 `02_brand` 제거 |
| 배속·정적 | 전편 `playback_speed` 1.25 · `silence_sec` 0.4 · 이름 뒤 1.0초 |
| 초상 | "전편 미확보" → **8편 중 6편 확보** |
| 제작 전 확인 | 13·17·18·20화 **전부 해결** |

> ⚠️ **전환하면서 사료를 다시 읽어야 한다 — 포맷만 갈아끼우면 안 된다.**
> 재대조에서 **사실 오류·충돌 열 건**이 나왔다. 대표적인 것:
> - **12화** '상하이 임시정부의 추천장' ← 실제는 **이시영·방성도·탕자오 개인 추천서 3장**
> - **13화** 생몰년이 공적정보에 **아예 없다**. 정책브리핑(1879.11.14)과 일본 자료(1880.11.13)가 어긋남 → **나이를 대본에서 뺐다**
> - **16화** '여든 살 노인' ← **만 78세**(세는나이)
> - **17화** '자신이 의열단임을 밝혔다' ← 백과사전 두 항목에 **없음** → 삭제
> - **20화** 전투 지명이 공적조서와 공훈록에서 **어긋남**(원봉/해창) → 전투 이름 전부 삭제
>
> **세는나이 실수가 11·12·16·18화에서 네 번 반복됐다.** 앞으로 나이는 **반드시 생년월일부터 다시 계산**하고,
> 생월일이 없으면 **연차로만** 쓴다(11·17·20화 방식).

**소제목 개정 (2026-09-21)**: 14·15·16·17·18·19화 여섯 편. 전 20편 폭을 `archive_layout`으로 실측했다(최대 888px / 한계 960px).

**⚠️ 마무리 문장이 같은 구문으로 몰리는지 본다.** v2 전환을 마치고 보니
13~19화 여섯 편이 전부 '**~한 지 N 만이었다**'로 끝났다. 11·12화까지 **아홉 편 중 여덟 편**이 같았다.
PRD 2항이 제시한 형태는 둘인데 한쪽만 쓴 것이다. → 네 가지로 분산했다:
(a) `~한 지 N 만이었다`(15·16) / (b) `~한 것은 N 전이었다`(13·19) /
(c) `~하고 N이 지나 있었다·N 뒤였다`(17·18) / (d) 그 밖(14·20).
**회차를 하나씩 쓰면 안 보이고, 다 쓴 뒤 마지막 문장만 세로로 훑어야 보인다.**

⚠️ `make_ending.py`의 `--pick`은 **`--takes`와 같이 줘야 한다.** 루프가
`range(1, takes+1)`이라 `--pick 3`만 주면 i=1에서 `continue`로 빠져 **아무 일도 일어나지 않고
exit 0으로 끝난다**. 올바른 호출: `make_ending.py 권기옥 --slug ep12_kwon --takes 3 --pick 3 --apply <대본>`

**남은 일**: 13~20화는 배경 이미지 생성(편당 10~11장) → TTS → 엔딩 → 렌더.

### 이번에 고친 것들

- **`make_ending.py` 캐시 버그** — 이름 음성을 `name{i}.wav`라는 고정 경로에 저장해,
  `--pick`을 주면 **앞 회차 이름이 그대로 재사용**됐다. 11화에 10화(이육사) 목소리가
  들어가 렌더까지 마쳤다. 경로에 인물명을 넣고, 조립 후 **다른 회차 엔딩과 md5를 비교해
  같으면 중단**하도록 막았다.
- **후보표 오류 2건** — 최익현 '단식 순국'(사료 없음), 안규홍 몰년 1911→1910.
  `candidates_dok.md`에 정정 기록을 남겼다.
- **수동 자막(SRT) 철회** — 유튜브가 자동 자막을 이미 인덱싱하므로 실익이 없다.

### 고정 방침

- **⛔ 자동 업로드 보류** (사용자 지시). `output/최종본/`까지만 만들고 멈춘다.
  `upload_youtube.py`는 이 채널에 없다.
- **⛔ 구글 사이트 브라우저 자동화 금지.** 유튜브·GCP 콘솔·스튜디오를 자동화하다
  계정이 봇으로 분류돼 정지된 적이 있다. 유튜브 자료가 필요하면 사용자에게 요청한다.
- 대본 JSON이 회차 설정을 전부 갖는다(`image`·`nameplate`·`credit`·`pause_after`·
  `playback_speed`·`silence_sec`·`output`·`audio_dir`). `build_episode.py`는 회차마다 고치지 않는다.
- `generate_tts.py`는 세 번째 인자로 **고친 문장만** 다시 뽑는다(§8 비결정성).

---

## 13. 폴더 구조

잡학쿠키와 산출물이 뒤섞여 헷갈렸던 문제 때문에 시리즈를 통째로 분리했다.

```
yt_1/
├── 이름을 부르다/          ← 이 시리즈 전용 (자기완결)
│   ├── PRD.md
│   ├── drafts/            대본 JSON, 인물 후보
│   ├── scripts/           시리즈 전용 파이프라인
│   ├── assets/            fonts(Noto Serif KR), photos(퍼블릭 도메인 초상)
│   └── output/
│       ├── images/        AI 생성 배경
│       ├── audio/         TTS, BGM, 각종 후보
│       └── 최종본/         ← 완성본만 (업로드 대상)
├── PRD.md                 잡학쿠키
├── drafts/ scripts/ output/   잡학쿠키
├── output/최종본/          잡학쿠키 완성본
└── .env, client_secret.json, token.json   (두 채널 공용)
```

스크립트는 전부 `BASE = scripts/..` 기준 상대경로라 시리즈 폴더째 옮겨도 동작한다.
`.env`는 최상단에 있으므로 시리즈 스크립트는 한 단계 위까지 찾아 올라간다.

---

## 14. 렌더 운영 (2026-09-11)

**한 편씩 돌린다.** 세 편을 `for`로 이어 돌리면 시스템 메모리 부족으로 죽는다.
편 사이에 프레임 잔여물(편당 약 2GB)을 지운다.

```bash
find output/video/_frames -name '*.png' -delete
python3 scripts/build_episode.py drafts/script_dok_03_ahn.json
```

> ⚠️ zsh에서 `rm -f output/video/_frames/*.png`는 **디렉터리가 비어 있으면
> "no matches found"로 실패**한다. `&&` 체인이 거기서 끊겨 렌더가 시작조차 안 한다.
> `find ... -delete`를 쓸 것.

**메모리 수정 이력**: `trans_drop()`이 전환 프레임 32장을 리스트로 만들어 들고 있었다.
1080×1920 RGB 32장이면 약 200MB다. `trans_drop_frame()`으로 한 장씩 만들어 쓰고
버리도록 바꿨다. 이후로는 안 죽는다.

렌더 중 다른 앱이 메모리를 많이 쓰면(크롬, Claude 세션 여러 개) 여전히 위험하다.
