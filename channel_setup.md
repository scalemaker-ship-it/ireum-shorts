# 「이름을 부르다」 채널 개설 자료

YouTube Studio에 그대로 붙여 넣을 수 있게 정리했다.
제작 규격은 `PRD.md`가 authoritative이고, 이 문서는 **채널 껍데기(브랜딩·메타데이터)** 만 다룬다.

> ⚠️ **구글 사이트는 사람이 직접 조작한다.** 계정 정지 이력이 있어
> Studio·계정 페이지를 자동화하지 않는다. 아래 값은 복사해서 손으로 넣는다.

---

## 1. 채널 기본 정보

| 항목 | 값 |
|---|---|
| 채널명 | **이름을 부르다** |
| 핸들 | 아래 후보 중 사용 가능한 것 |
| 국가 | 대한민국 |
| 언어 | 한국어 |
| 카테고리 | 교육 / 인물·블로그 |
| 아동용 여부 | **아동용 아님** (채널 기본값으로 설정) |
| 비즈니스 문의 이메일 | (직접 입력 — 공개 노출되는 칸이다) |

### 핸들 후보

핸들은 나중에 바꾸면 기존 링크가 깨지므로 처음에 정한다.
한글 핸들은 거절될 수 있어 로마자 후보를 먼저 둔다.

1. `@ireum-bureuda` — 채널명 로마자 표기, 가장 직관적
2. `@callthename` — 영문 의미 그대로, 짧고 외국 시청자도 읽는다
3. `@ireumeul` — 짧게 줄인 판본
4. `@bureuda` — 더 짧게. 다만 채널명 유추가 어렵다
5. `@이름을부르다` — 허용되면 최선. 안 되면 1번

사용 가능 여부는 `youtube.com/@후보` 를 직접 열어 확인한다(없는 핸들이면 404).

---

## 2. 채널 설명 (1,000자 제한 / 아래 본문 388자)

```
우리는 3·1운동을 알고 광복절을 안다.
그러나 그 사이에 서 있던 사람들의 이름은 대부분 모른다.

이 채널은 독립운동가 한 사람의 이야기를 1분에 담는다.
다만 이름은 맨 마지막에 부른다.
그가 무엇을 했는지 먼저 보고, 그가 누구였는지 나중에 안다.
이름을 먼저 말하면 아는 사람과 모르는 사람으로 갈리지만,
이야기를 먼저 들으면 모두가 같은 자리에서 그 이름을 처음 만난다.

- 매일 한 편, 한 사람
- 사실은 국가보훈부 공훈전자사료관과 한국민족문화대백과사전에서 확인한 것만 쓴다
- 초상은 저작권이 만료됐거나 공공누리로 개방된 실사진만 쓰고,
  실존 인물의 얼굴을 AI로 만들지 않는다
- 근거 출처는 편마다 설명란에 밝힌다

다음에 부르고 싶은 이름이 있다면 댓글로 남겨 달라.
```

**왜 이렇게 썼는가**
- 첫 두 줄이 훅이다. 채널 설명도 검색 결과·공유 카드에 앞부분만 노출된다.
- 포맷(이름을 마지막에)을 설명해 둬야 시청자가 "제목에 이름이 없는 이유"를 이해한다.
- 검증·초상 규칙을 공개한다. 실존 인물 채널은 신뢰가 구독 사유가 되고,
  나중에 사실 관계 시비가 붙었을 때 기준을 미리 밝혀 둔 쪽이 방어에 유리하다.
- 톤은 영상과 같은 `~했다/~달라` 평서체다. 음슴체·해요체를 섞지 않는다.

---

## 3. 채널 키워드 (500자 제한)

```
독립운동가, 한국사, 근현대사, 역사 숏츠, 항일운동, 의열단, 광복군, 대한민국임시정부,
순국선열, 독립유공자, 의병, 여성 독립운동가, 만주 독립운동, 3·1운동, 안중근, 역사 교육,
한국사 인물, 기억, 이름을 부르다, korean history, independence movement, korean independence activist
```

인물 개인 이름은 키워드에 한 명(가장 검색량이 큰 안중근)만 두고 나머지는 편별 태그로 돌린다.
채널 키워드는 채널 전체 주제를 알고리즘에 알리는 칸이지 검색 노출 칸이 아니다.

---

## 4. 시각 자산

전부 영상과 같은 `archive_layout` 색·질감 위에서 만들었다. 재생성은
`python3 scripts/make_channel_art.py` (프로필) / `python3 scripts/make_channel_banner.py` (배너·워터마크).

| 자산 | 파일 | 규격 | 용량 |
|---|---|---|---|
| 프로필 사진 | `assets/channel_avatar.png` | 800×800 | 0.42MB (제한 4MB) |
| 배너 | `assets/channel_banner.png` | 2560×1440 | 2.41MB (제한 6MB) |
| 워터마크 | `assets/channel_watermark.png` | 150×150, 투명 PNG | 0.01MB (제한 1MB) |
| 재생목록 커버 ×8 | `assets/playlists/*_1080x1920.png` | 1080×1920 | 최대 1.38MB (제한 2MB) |
| 재생목록 커버 ×8 (가로) | `assets/playlists/*_1280x720.png` | 1280×720 | 최대 0.67MB |

확인용 미리보기: `output/channel_avatar_preview.png`(원형 크롭 + 48px 축소),
`output/channel_banner_preview.png`(모바일·태블릿·데스크톱·TV 크롭 비교),
`output/playlist_covers_preview.png` / `_h_preview.png`(8장 한 판 비교).

**배너 설계** — 유튜브는 한 장을 기기마다 다르게 자른다.
글자는 전부 공통 안전영역(중앙 1546×423) 안에 뒀고,
바깥 날개에는 **이름이 적히지 않은 빈 명패**를 깔았다.
잘려도 정보가 사라지지 않고, TV·데스크톱에서는 채널 포맷(마지막에 이름을 부른다)이
배경으로 읽힌다. 실존 인물 얼굴을 쓸 수 없다는 제약(PRD 5항)을 컨셉으로 바꾼 것이다.

배너 문구
- 상단 소제목: `독립운동가 한 사람의 마지막 이야기`
- 제목: `이름을 부르다`
- 하단: `우리가 부르지 않으면 사라지는 이름들 · 매일 한 편`

> 업로드 주기를 매일로 확정하기 전이라면 하단 문구에서 `· 매일 한 편`을 뺀다.
> `make_channel_banner.py`의 `SUB` 상수 한 줄만 고치면 된다.
> 지키지 못할 주기를 배너에 박아 두는 쪽이 손해다.

**워터마크 주의** — 유튜브 브랜딩 워터마크는 **숏츠 플레이어에 표시되지 않는다.**
지금은 롱폼(묶음편·사건편을 길게 낼 때)을 대비한 자산이다. 등록해 둬도 손해는 없다.

---

## 5. 재생목록

숏츠 채널도 재생목록이 있어야 "구독할 만한 채널"로 보인다. 개설 시 만들어 둔다.
분류는 PRD의 유형 로테이션(의열→여성→외국인→의병→문화→만주)과 같게 맞췄다.

| 재생목록 | 설명란 |
|---|---|
| **전부 부른 이름** | 지금까지 부른 모든 이름. 올라온 순서대로. |
| **폭탄을 든 사람들** | 의열 투쟁. 한 사람이 한 번의 기회에 모든 것을 걸었다. |
| **그 여자의 이름** | 여성 독립운동가. 기록이 가장 적게 남은 자리다. |
| **조선을 도운 이방인** | 조선 사람이 아니었으나 조선을 위해 싸운 사람들. |
| **나라가 무너질 때 총을 든 사람들** | 의병. 군인이 아니었던 이들이 먼저 싸웠다. |
| **붓과 노래로 싸웠다** | 언론·교육·문화·종교로 싸운 사람들. |
| **만주와 연해주에서** | 국경을 넘어 싸운 사람들. |
| **함께 불러야 할 이름들** | 한 편에 세 사람. 혼자로는 1분을 채울 수 없어도 잊혀선 안 되는 이름들. |

### 재생목록 커버 (`assets/playlists/`)

⚠️ **유튜브에는 재생목록에 이미지를 직접 올리는 칸이 없다.** 재생목록 썸네일은
그 안에 든 동영상 중 하나에서 고르게 되어 있다. 그래서 커버는 이렇게 쓴다.

1. **세로 1080×1920** — 해당 재생목록 **첫 편(또는 대표 편)의 맞춤 썸네일**로 올린다.
   그 썸네일이 곧 재생목록 표지로 노출된다. **이게 실제 경로다.**
2. **가로 1280×720** — 커뮤니티 게시물, 외부 공유, 가로 썸네일을 요구하는 자리용.

| 파일 | 재생목록 |
|---|---|
| `00_all` | 전부 부른 이름 |
| `01_uiyeol` | 폭탄을 든 사람들 |
| `02_women` | 그 여자의 이름 |
| `03_foreign` | 조선을 도운 이방인 |
| `04_uibyeong` | 나라가 무너질 때 총을 든 사람들 |
| `05_culture` | 붓과 노래로 싸웠다 |
| `06_manju` | 만주와 연해주에서 |
| `07_group` | 함께 불러야 할 이름들 |

**8장은 한 벌이다.** 제목 크기를 편마다 맞추면 나란히 놓았을 때 글자가 들쭉날쭉해
세트로 안 읽힌다. 가장 긴 제목이 들어가는 크기 하나를 구해 전부에 쓴다
(세로 126px / 가로 116px). 줄바꿈도 의미 단위로 직접 넣었다 —
자동 줄바꿈에 맡기면 `나라가 무너질 / 때 / 총을 든 사람들`처럼 끊긴다.

재생성: `python3 scripts/make_playlist_covers.py`
(제목·설명은 스크립트 상단 `PLAYLISTS` 한 곳에서만 고친다)

> **주의**: 커버를 첫 편의 썸네일로 쓰면 그 영상은 썸네일에 이름이 없는 대신
> 후킹 이미지도 없다. 조회수가 붙은 편에는 걸지 말고, 재생목록 진입용으로만 쓴다.

---

## 6. 업로드 기본값 (Studio → 설정 → 업로드 기본 설정)

### 제목

`{영상 상단 소제목과 같은 후킹 질문}` + ` #shorts`
예) `누가 총독에게 폭탄을 던졌나 #shorts`

**제목에 인물 이름·나이·직업을 넣지 않는다.** 반전이 통째로 깨진다.
([[feedback_script_twist_structure]] — 이 채널에선 제목·썸네일·설명 첫 줄까지 적용)

### 설명란 템플릿

```
{제목과 같은 질문 한 줄}

독립운동가 한 사람의 이야기를 1분에 담습니다. 이름은 마지막에 부릅니다.
다음에 부르고 싶은 이름을 댓글로 남겨 주세요.

──────────────
※ 아래에 이 편 인물의 이름이 있습니다.

인물: OOO (1855~1920)
서훈: 건국훈장 OOO장 (19OO년)

출처
· 국가보훈부 공훈전자사료관, 독립유공자 공훈록 O권
· 한국민족문화대백과사전 「OOO」
· 초상: {출처} ({퍼블릭 도메인 / 공공누리 제1유형})

#독립운동가 #한국사 #근현대사 #OOO
```

- 이름을 **구분선 아래**로 내린다. 설명란은 접힌 상태에서 앞 1~2줄만 보이므로
  반전은 지키면서 인물명 검색 노출은 그대로 가져간다.
- 출처 표기는 선택이 아니다. 공공누리 제1유형은 **출처 표시가 이용 조건**이고,
  사실 관계 시비가 붙었을 때 근거가 된다.

### 태그

`독립운동가, 한국사, 근현대사, 역사, 항일운동, 순국선열, {인물명}, {소속·사건명}, shorts`

태그는 시청자에게 보이지 않으므로 **인물명을 넣어도 반전이 깨지지 않는다.**

### 고정 댓글 (업로드 후 직접 고정)

```
다음에 부르고 싶은 이름을 댓글로 남겨주세요.
```

나레이션에서 CTA를 뺀 이유가 여기 있다 — 순국 이야기 직후의 구독 요청은 톤을 깬다(PRD 2항).

---

## 7. 개설 순서 체크리스트

1. [ ] 브랜드 계정으로 채널 생성 (개인 계정과 분리해야 나중에 권한 위임·양도가 된다)
2. [ ] 채널명 `이름을 부르다`, 핸들 확정
3. [ ] 프로필 사진 `assets/channel_avatar.png` 업로드
4. [ ] 배너 `assets/channel_banner.png` 업로드 → **모바일 미리보기에서 글자 잘림 확인**
5. [ ] 워터마크 `assets/channel_watermark.png` 등록 (표시 시점: 동영상 전체)
6. [ ] 채널 설명·키워드(2·3항) 붙여넣기
7. [ ] 국가 대한민국 / 언어 한국어 / **아동용 아님** 기본값
8. [ ] 재생목록 8개 생성 (5항) — 커버는 각 재생목록 첫 편의 맞춤 썸네일로 올린다
9. [ ] 업로드 기본 설정에 설명란 템플릿·태그 저장 (6항)
10. [ ] 2단계 인증 → 15분 이상 업로드·맞춤 썸네일 권한 활성화
11. [ ] 재고 14편이 쌓인 뒤 첫 업로드 (PRD 11항 — 매일 업로드는 재고로 버틴다)

### 아직 하지 않아도 되는 것

- **채널 예고편**: 구독자 0명 구간에서 효과가 거의 없다. 영상이 10편쯤 쌓이고
  가장 반응이 좋은 편을 예고편으로 거는 편이 낫다.
- **수익 창출 신청**: 조건(구독 1,000 + 숏츠 조회 1,000만/90일) 미달이다.
  다만 **지금부터 나무위키 문장과 타인 음원을 쓰지 않는 이유**가 이것이다.
  나중에 갈아엎을 수 없다.
- **커뮤니티 탭·스토어·멤버십**: 해당 없음.

---

## 8. 운영상 미리 알아 둘 것

- **광고 적합성**: 고문·처형·순국을 다루므로 "논란의 소지가 있는 문제" 항목에서
  제한적 광고 판정이 날 수 있다. 사료 표현을 그대로 쓰고 자극적 묘사를 더하지 않는 것이
  톤 문제이자 수익 문제다.
- **저작권**: 나무위키 문장 금지(NC 조항), 타인 음원 금지(BGM 직접 합성) — PRD 3·8항.
  채널 단위로 한 번 걸리면 개별 영상보다 회복이 오래 걸린다.
- **AI 콘텐츠 라벨**: 배경·상징물 이미지에 AI 생성물을 쓰므로 업로드 시
  "변경되거나 합성된 콘텐츠" 고지 여부를 매번 판단한다. 실존 인물의 얼굴·발언을
  합성하지 않으므로(PRD 5항) 필수 공개 대상은 아니지만, 기준이 바뀔 수 있다.
- **댓글**: 인물·역사 해석을 둘러싼 논쟁이 붙기 쉽다. 검토를 위해 보류할 단어에
  이념 대립 관련어를 미리 넣어 두면 초반 관리 부담이 준다.
